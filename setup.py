from setuptools import setup, find_packages

setup(
    name='kalasanty',
    version='0.0.1',
    description='Detecting druggable pockets in protein structures',
    url='https://gitlab.com/cheminfibb/kalasanty',
    packages=find_packages(include=["kalasanty"]),
    install_requires=[],
)
